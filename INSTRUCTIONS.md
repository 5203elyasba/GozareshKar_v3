# راهنمای راه‌اندازی کامل پروژه (نسخه نهایی)

سلام. پروژه به صورت کامل و از ابتدا ساخته شده است. برای راه‌اندازی آن روی سیستم خودتان با XAMPP، لطفاً مراحل زیر را به ترتیب دنبال کنید.

## مرحله ۱: قرار دادن فایل‌های پروژه

1.  فایل‌های جدید پروژه را که دریافت می‌کنید، در یک پوشه (مثلاً به نام `time-tracker`) قرار دهید.
2.  این پوشه (`time-tracker`) را به مسیر `htdocs` در محل نصب XAMPP خود منتقل کنید.
    *   مسیر معمولاً به این شکل است: `C:\xampp\htdocs\`

## مرحله ۲: فعال‌سازی افزونه PHP (بسیار مهم)

1.  کنترل پنل XAMPP را باز کنید.
2.  کنار گزینه‌ی **Apache**، روی دکمه **Config** کلیک کرده و **PHP (php.ini)** را انتخاب کنید.
3.  در فایل باز شده، به دنبال خط `;extension=intl` بگردید و **نقطه ویرگول (`;`)** را از ابتدای آن حذف کنید تا به صورت `extension=intl` درآید.
4.  فایل را ذخیره کرده و **سرویس Apache را یک بار Stop و سپس Start کنید.** این مرحله برای کار با تاریخ شمسی ضروری است.

## مرحله ۳: راه‌اندازی دیتابیس

1.  در مرورگر خود آدرس `http://localhost/phpmyadmin` را باز کنید.
2.  فایل `database.sql` که در پوشه اصلی پروژه قرار دارد، همه کارها را برای شما انجام می‌دهد. کافیست به تب `Import` بروید.
3.  روی `Choose File` کلیک کرده و فایل `database.sql` را انتخاب کنید.
4.  در پایین صفحه، روی دکمه `Go` کلیک کنید. این اسکریپت به صورت خودکار دیتابیس `employee_tracker` و تمام جداول مورد نیاز را می‌سازد و کاربران تستی را نیز اضافه می‌کند.

## مرحله ۴: اجرای پروژه

1.  اکنون مرورگر خود را باز کرده و آدرس زیر را در آن وارد کنید:
    `http://localhost/time-tracker/login.php`
    *(توجه: اگر نام پوشه را چیز دیگری گذاشته‌اید، `time-tracker` را با همان نام جایگزین کنید.)*

## اطلاعات ورود

شما می‌توانید با یکی از دو کاربر زیر وارد شوید:

*   **کاربر ادمین:**
    *   **نام کاربری:** `admin`
    *   **رمز عبور:** `password`
    *   (ادمین به پنل مدیریت برای تنظیم ساعات کاربران دسترسی دارد)

*   **کاربر عادی:**
    *   **نام کاربری:** `testuser`
    *   **رمز عبور:** `password`

اگر در هر مرحله به مشکلی برخوردید، به من اطلاع دهید.
