# راهنمای راه‌اندازی و اجرای پروژه

برای اجرای پروژه روی سیستم خودتان با XAMPP، لطفاً مراحل زیر را به ترتیب دنبال کنید.

## مرحله ۱: کپی کردن فایل‌های پروژه

1.  ابتدا تمام فایل‌ها و پوشه‌های پروژه را در یک پوشه (مثلاً به نام `my-project`) قرار دهید.
2.  این پوشه (`my-project`) را به مسیر `htdocs` در محل نصب XAMPP خود منتقل کنید.
    *   مسیر معمولاً به این شکل است: `C:\xampp\htdocs\`

## مرحله ۲: روشن کردن XAMPP

1.  کنترل پنل XAMPP را باز کنید.
2.  سرویس‌های **Apache** و **MySQL** را با کلیک بر روی دکمه **Start** برای هر دو، فعال کنید.

## مرحله ۳: ساخت دیتابیس

1.  در مرورگر خود آدرس `http://localhost/phpmyadmin` را باز کنید.
2.  در صفحه‌ی باز شده، روی `New` در منوی سمت چپ کلیک کنید تا یک دیتابیس جدید بسازید.
3.  نام دیتابیس را `employee_tracker` بگذارید.
4.  برای پشتیبانی کامل از زبان فارسی، از منوی `Collation` گزینه `utf8mb4_persian_ci` را انتخاب کنید.
5.  روی دکمه `Create` کلیک کنید.

## مرحله ۴: وارد کردن جداول (Import)

1.  پس از ساخت دیتابیس، آن را از لیست سمت چپ انتخاب کنید.
2.  به تب `Import` در بالای صفحه بروید.
3.  روی دکمه `Choose File` کلیک کرده و فایل `database.sql` را که در پوشه اصلی پروژه قرار دارد، انتخاب کنید.
4.  در پایین صفحه، روی دکمه `Import` (یا `Go`) کلیک کنید. پس از چند لحظه، جداول `users` و `time_logs` به دیتابیس شما اضافه خواهند شد.

## مرحله ۵: ساخت کاربر تستی برای ورود

1.  دوباره دیتابیس `employee_tracker` را از لیست سمت چپ انتخاب کنید.
2.  به تب `SQL` بروید.
3.  کد زیر را به طور کامل کپی کرده و در کادر متنی قرار دهید و سپس روی دکمه `Go` کلیک کنید. این کد یک کاربر تستی برای شما ایجاد می‌کند تا بتوانید وارد سیستم شوید:

    ```sql
    INSERT INTO `users` (`username`, `password`, `full_name`, `role`) VALUES
    ('testuser', '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi', 'کاربر تستی', 'employee');
    ```
    *   **نام کاربری:** `testuser`
    *   **رمز عبور:** `password`

## مرحله ۶: اجرای نهایی پروژه

1.  اکنون مرورگر خود را باز کرده و آدرس زیر را در آن وارد کنید:
    `http://localhost/my-project/public/login.php`
    *(توجه: اگر نام پوشه را چیز دیگری گذاشته‌اید، `my-project` را با همان نام جایگزین کنید.)*

صفحه‌ی ورود به سیستم برای شما نمایش داده می‌شود و می‌توانید با اطلاعات کاربر تستی که ساختید، وارد شوید و تمام امکانات را تست کنید. اگر در هر مرحله به مشکلی برخوردید، به من اطلاع دهید.
